# ✅ Чеклист для деплоя на хостинг

## 📋 Что нужно подготовить перед деплоем

### 1. Учетные данные и токены

- [ ] **BOT_TOKEN** - токен Telegram бота от @BotFather
- [ ] **DEEPSEEK_API_KEY** - API ключ DeepSeek (или YANDEX_GPT_API_KEY)
- [ ] **YANDEX_GPT_FOLDER_ID** - если используете Yandex GPT (опционально)
- [ ] **SENTRY_DSN** - для мониторинга ошибок (опционально)

### 2. Информация о хостинге

- [ ] **Тип хостинга**: Kubernetes кластер / VPS / Облачный провайдер (GKE, EKS, AKS)
- [ ] **Доступ к кластеру**: есть ли у вас `kubectl` настроен?
- [ ] **Container Registry**: куда публиковать Docker образ (Docker Hub, GHCR, GCR, ECR, ACR)
- [ ] **Доменное имя**: если нужен webhook для Telegram

### 3. Настройки базы данных

- [ ] **POSTGRES_PASSWORD** - придумайте безопасный пароль
- [ ] **Storage Class** - для Kubernetes PVC (проверьте доступные в кластере)

### 4. Проверка перед деплоем

- [ ] Все изменения закоммичены в git
- [ ] Docker образ собран и протестирован локально
- [ ] Секреты заполнены в `k8s/bot-secret.yaml` и `k8s/postgres-secret.yaml`
- [ ] Образ указан в `k8s/bot-deployment.yaml` и `k8s/job-migrations.yaml`

## 🚀 Шаги деплоя

### Шаг 1: Подготовка секретов

Отредактируйте файлы:
- `k8s/postgres-secret.yaml` - установите безопасный пароль
- `k8s/bot-secret.yaml` - заполните все токены и API ключи

### Шаг 2: Сборка и публикация образа

```bash
# Соберите образ
docker build -t your-registry/cashback-bot:latest .

# Опубликуйте в registry
docker push your-registry/cashback-bot:latest
```

### Шаг 3: Обновление манифестов

Обновите `k8s/bot-deployment.yaml` и `k8s/job-migrations.yaml`:
- Замените `cashback-bot:latest` на ваш образ из registry

### Шаг 4: Деплой в Kubernetes

```bash
# Применить все манифесты
kubectl apply -k k8s/

# Проверить статус
kubectl get pods -n cashback-bot
kubectl logs -f deployment/cashback-bot -n cashback-bot
```

## ❓ Вопросы для уточнения

Пожалуйста, ответьте на следующие вопросы:

1. **Какой тип хостинга?**
   - [ ] Kubernetes кластер (GKE, EKS, AKS, или свой)
   - [ ] VPS сервер (нужно будет установить Kubernetes)
   - [ ] Другой вариант

2. **Есть ли доступ к Kubernetes?**
   - [ ] Да, `kubectl` уже настроен
   - [ ] Нет, нужно настроить

3. **Куда публиковать Docker образ?**
   - [ ] Docker Hub
   - [ ] GitHub Container Registry (GHCR)
   - [ ] Google Container Registry (GCR)
   - [ ] Amazon ECR
   - [ ] Azure Container Registry (ACR)
   - [ ] Другой registry

4. **Есть ли все токены и API ключи?**
   - [ ] Да, все готово
   - [ ] Нет, нужно получить

5. **Нужен ли webhook для Telegram?**
   - [ ] Да, есть домен
   - [ ] Нет, будет использоваться polling

